'use client'

// Custom providers for the blog application
// NextAuth removed - using custom JWT authentication

export default function Providers({ children }) {
  return (
    <>
      {children}
    </>
  )
}
